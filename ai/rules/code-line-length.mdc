---
description: 代码行默认不超过 120 列
alwaysApply: true
enabled: true
updatedAt: 2025-12-31T03:05:13.036Z
provider: 
---

# 代码行长度规范

默认所有代码行不超过 `120` 个字符。

## 适用范围

- Kotlin / Java
- TypeScript / JavaScript
- Vue
- Go
- SQL

## 强约束

- 超长时必须主动换行，不要把可读性赌给横向滚动。
- 优先拆分参数列表、链式调用、长字符串和复杂表达式。
- 新增代码时就按 `120` 列组织，不要依赖事后返工。

## 例外

- `package` / `import`
- 不可拆分的 URL
- 不可拆分的正则表达式
- 版权头等固定声明

## 注意事项

- 具体怎么换行，优先遵循对应语言的 formatter 和相关 skill。
- 这条 rule 只定义长度上限，不承担各语言的完整格式化教程。