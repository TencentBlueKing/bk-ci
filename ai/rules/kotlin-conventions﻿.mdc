---
description: Kotlin 高频硬约束
type: always
---
# Kotlin 高频硬约束

- Kotlin 的完整项目规范请看 `kotlin-backend-conventions`，本 rule 只保留高频、低噪声、强约束内容。
- 单行长度不超过 120 个字符；`package` 和 `import` 例外。
- 使用 4 个空格缩进，禁止 Tab。
- 禁止 wildcard import，清理未使用 import。
- 多个容易混淆的同类参数调用，优先使用命名参数；data class 实例化优先显式命名参数。
- 禁止直接使用完整类路径调用方法，先 import 再使用简洁类名。
- 优先用 Kotlin 空安全语法表达可空性；`!!` 只能在有充分理由时使用。
- 代码应保持可读和可维护，避免巨型函数、巨型类和深层嵌套。
