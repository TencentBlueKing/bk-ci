# dispatchPcg hpa
{{ if .Values.dispatchPcg.enabled -}}
{{ if .Values.dispatchPcg.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-dispatch-pcg
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: dispatch-pcg
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-dispatch-pcg
  minReplicas: {{ .Values.dispatchPcg.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dispatchPcg.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.dispatchPcg.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.dispatchPcg.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.dispatchPcg.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.dispatchPcg.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
