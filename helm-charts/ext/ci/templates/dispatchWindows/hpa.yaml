# dispatchWindows hpa
{{ if .Values.dispatchWindows.enabled -}}
{{ if .Values.dispatchWindows.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-dispatch-windows
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: dispatch-windows
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-dispatch-windows
  minReplicas: {{ .Values.dispatchWindows.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dispatchWindows.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.dispatchWindows.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.dispatchWindows.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.dispatchWindows.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.dispatchWindows.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
