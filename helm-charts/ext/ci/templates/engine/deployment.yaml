#engine Deployment
{{ if .Values.engine.enabled -}}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "bkci.names.fullname" . }}-engine
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: microservice
    app.kubernetes.io/name: engine
spec:
  selector:
    matchLabels: {{- include "bkci.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: microservice
      app.kubernetes.io/name: engine
  {{- if not .Values.engine.autoscaling.enabled }}
  replicas: {{ .Values.engine.replicas }}
  {{- end }}
  template:
    metadata:
      labels: {{- include "bkci.labels.standard" . | nindent 8 }}
        {{- if .Values.engine.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" .Values.engine.podLabels "context" $) | nindent 8 }}
        {{- end }}
        app.kubernetes.io/component: microservice
        app.kubernetes.io/name: engine
    spec:
      serviceAccountName: {{ .Values.rbac.serviceAccount }}
      {{- if .Values.engine.hostAliases }}
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.engine.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.engine.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.engine.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.engine.podAffinityPreset  "context" $) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.engine.podAntiAffinityPreset  "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.engine.nodeAffinityPreset.type "key" .Values.engine.nodeAffinityPreset.key "values" .Values.engine.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.engine.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.engine.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.engine.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.engine.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.engine.priorityClassName }}
      priorityClassName: {{ .Values.engine.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.engine.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.engine.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      containers:
        - name: engine
          image: {{ include "common.images.image" ( dict "imageRoot" .Values.backendImage "global" $) }}
          imagePullPolicy: {{ .Values.backendImage.pullPolicy }}
          {{- if .Values.engine.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.engine.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.engine.resources }}
          resources: {{- toYaml .Values.engine.resources | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 80
          env:
            - name: MS_NAME
              value: engine
            - name: NAMESPACE
              value: {{ .Release.Namespace }}
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: CHART_NAME
              value: {{ .Chart.Name }}
            - name: MULTI_CLUSTER
              value: {{ .Values.multiCluster.enabled | quote }}
            - name: DEFAULT_NAMESPACE
              value: {{ .Values.multiCluster.defaultNamespace }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
          workingDir: /data/workspace/engine
          livenessProbe:
            httpGet:
              path: /management/health/livenessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /management/health/readinessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          volumeMounts:
          - mountPath: /data/workspace/engine/jvm
            name: log-volume
            subPathExpr: bkci/jvm/$(POD_NAME)
      volumes:
      - hostPath:
          path: /data
        name: log-volume
{{- end -}}
