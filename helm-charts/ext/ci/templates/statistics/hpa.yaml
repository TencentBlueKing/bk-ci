# statistics hpa
{{ if .Values.statistics.enabled -}}
{{ if .Values.statistics.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-statistics
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: statistics
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-statistics
  minReplicas: {{ .Values.statistics.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.statistics.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.statistics.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.statistics.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.statistics.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.statistics.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
