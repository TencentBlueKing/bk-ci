#experience Deployment
{{ if .Values.experience.enabled -}}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "bkci.names.fullname" . }}-experience
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: microservice
    app.kubernetes.io/name: experience
spec:
  selector:
    matchLabels: {{- include "bkci.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: microservice
      app.kubernetes.io/name: experience
  {{- if not .Values.experience.autoscaling.enabled }}
  replicas: {{ .Values.experience.replicas }}
  {{- end }}
  template:
    metadata:
      labels: {{- include "bkci.labels.standard" . | nindent 8 }}
        {{- if .Values.experience.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" .Values.experience.podLabels "context" $) | nindent 8 }}
        {{- end }}
        app.kubernetes.io/component: microservice
        app.kubernetes.io/name: experience
    spec:
      serviceAccountName: {{ .Values.rbac.serviceAccount }}
      {{- if .Values.experience.hostAliases }}
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.experience.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.experience.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.experience.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.experience.podAffinityPreset  "context" $) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.experience.podAntiAffinityPreset  "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.experience.nodeAffinityPreset.type "key" .Values.experience.nodeAffinityPreset.key "values" .Values.experience.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.experience.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.experience.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.experience.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.experience.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.experience.priorityClassName }}
      priorityClassName: {{ .Values.experience.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.experience.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.experience.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}        
      containers:
        - name: experience
          image: {{ include "common.images.image" ( dict "imageRoot" .Values.backendImage "global" $) }}
          imagePullPolicy: {{ .Values.backendImage.pullPolicy }}
          {{- if .Values.experience.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.experience.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.experience.resources }}
          resources: {{- toYaml .Values.experience.resources | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 80
          env:
            - name: MS_NAME
              value: experience
            - name: NAMESPACE
              value: {{ .Release.Namespace }}
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: CHART_NAME
              value: {{ .Chart.Name }}
            - name: MULTI_CLUSTER
              value: {{ .Values.multiCluster.enabled | quote }}
            - name: DEFAULT_NAMESPACE
              value: {{ .Values.multiCluster.defaultNamespace }}
            - name: JAVA_TOOL_OPTIONS
              value: {{ .Value.java.options }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
          workingDir: /data/workspace/experience
          livenessProbe:
            httpGet:
              path: /management/health/livenessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /management/health/readinessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          volumeMounts:
          - mountPath: /data/workspace/experience/jvm
            name: log-volume
            subPathExpr: bkci/jvm/$(POD_NAME)
      volumes:
      - hostPath:
          path: /data
        name: log-volume
{{- end -}}
