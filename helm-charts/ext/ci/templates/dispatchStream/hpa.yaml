# dispatchStream hpa
{{ if .Values.dispatchStream.enabled -}}
{{ if .Values.dispatchStream.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-dispatch-stream
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: dispatch-stream
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-dispatch-stream
  minReplicas: {{ .Values.dispatchStream.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dispatchStream.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.dispatchStream.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.dispatchStream.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.dispatchStream.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.dispatchStream.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
