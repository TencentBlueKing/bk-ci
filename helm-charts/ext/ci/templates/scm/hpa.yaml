# scm hpa
{{ if .Values.scm.enabled -}}
{{ if .Values.scm.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-scm
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: scm
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-scm
  minReplicas: {{ .Values.scm.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.scm.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.scm.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.scm.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.scm.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.scm.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
