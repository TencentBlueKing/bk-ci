#dispatchCodecc Deployment
{{ if .Values.dispatchCodecc.enabled -}}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "bkci.names.fullname" . }}-dispatch-codecc
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: microservice
    app.kubernetes.io/name: dispatch-codecc
spec:
  selector:
    matchLabels: {{- include "bkci.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: microservice
      app.kubernetes.io/name: dispatch-codecc
  {{- if not .Values.dispatchCodecc.autoscaling.enabled }}
  replicas: {{ .Values.dispatchCodecc.replicas }}
  {{- end }}
  template:
    metadata:
      labels: {{- include "bkci.labels.standard" . | nindent 8 }}
        {{- if .Values.dispatchCodecc.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" .Values.dispatchCodecc.podLabels "context" $) | nindent 8 }}
        {{- end }}
        app.kubernetes.io/component: microservice
        app.kubernetes.io/name: dispatch-codecc
    spec:
      serviceAccountName: {{ .Values.rbac.serviceAccount }}
      {{- if .Values.dispatchCodecc.hostAliases }}
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.dispatchCodecc.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.dispatchCodecc.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.dispatchCodecc.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.dispatchCodecc.podAffinityPreset "context" $) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.dispatchCodecc.podAntiAffinityPreset  "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.dispatchCodecc.nodeAffinityPreset.type "key" .Values.dispatchCodecc.nodeAffinityPreset.key "values" .Values.dispatchCodecc.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.dispatchCodecc.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.dispatchCodecc.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.dispatchCodecc.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.dispatchCodecc.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.dispatchCodecc.priorityClassName }}
      priorityClassName: {{ .Values.dispatchCodecc.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.dispatchCodecc.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.dispatchCodecc.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}        
      containers:
        - name: dispatch-codecc
          image: {{ include "common.images.image" ( dict "imageRoot" .Values.backendImage "global" $) }}
          imagePullPolicy: {{ .Values.backendImage.pullPolicy }}
          {{- if .Values.dispatchCodecc.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.dispatchCodecc.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.dispatchCodecc.resources }}
          resources: {{- toYaml .Values.dispatchCodecc.resources | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 80
          env:
            - name: MS_NAME
              value: dispatch-codecc
            - name: NAMESPACE
              value: {{ .Release.Namespace }}
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: CHART_NAME
              value: {{ .Chart.Name }}
            - name: MULTI_CLUSTER
              value: {{ .Values.multiCluster.enabled | quote }}
            - name: DEFAULT_NAMESPACE
              value: {{ .Values.multiCluster.defaultNamespace }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
          workingDir: /data/workspace/dispatch-codecc
          livenessProbe:
            httpGet:
              path: /management/health/livenessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /management/health/readinessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          volumeMounts:
          - mountPath: /data/workspace/dispatch-codecc/jvm
            name: log-volume
            subPathExpr: bkci/jvm/$(POD_NAME)
      volumes:
      - hostPath:
          path: /data
        name: log-volume
{{- end -}}
