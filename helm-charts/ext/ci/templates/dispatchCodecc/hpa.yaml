# dispatchCodecc hpa
{{ if .Values.dispatchCodecc.enabled -}}
{{ if .Values.dispatchCodecc.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bkci.names.fullname" . }}-dispatch-codecc
  labels: {{- include "bkci.labels.standard" . | nindent 4 }}
    app.kubernetes.io/name: dispatch-codecc
    app.kubernetes.io/component: microservice
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "bkci.names.fullname" . }}-dispatch-codecc
  minReplicas: {{ .Values.dispatchCodecc.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dispatchCodecc.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.dispatchCodecc.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.dispatchCodecc.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.dispatchCodecc.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.dispatchCodecc.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
{{- end -}}
