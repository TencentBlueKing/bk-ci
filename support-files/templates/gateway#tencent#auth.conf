# service层验证IP白名单
location = /auth/service {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_service.lua';
}

# User层验证身份
location = /auth/user {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_user-__BK_CI_AUTH_PROVIDER__.lua';
}

# App层验证身份
location = /auth/app {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_app.lua';
}

# OP层验证身份
location = /auth/op {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_op.lua';
}

# 构建机层验证身份
location = /auth/build {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_build.lua';
}

# 设置灰度信息
location = /auth/gray {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_gray.lua';
}

# 验证prebuild身份
location = /auth/prebuild {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_prebuild.lua';
}

# 验证离岸开发人员身份
location = /auth/desktop {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_desktop.lua';
}

# 验证token
location = /auth/token {
    internal;
    set $auth_location "1";
    content_by_lua_file 'conf/lua/auth/auth_token.lua';
}
