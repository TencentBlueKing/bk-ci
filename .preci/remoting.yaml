version: 1.0.0

image: mirrors.tencent.com/ci/remote-dev-full-remoting:latest

vscode:
  extensions:
    - golang.go-nightly
    - eamodio.gitlens

envs:
  # NAME: xxxx
  # MY_PASSWORD: ${{ preci.envs.PROJECT_PASSWORD }}

ports:
- name: web-ide
  desc: webIde预览端口
  port: 23000
- name: api
  desc: api端口
  port: 22999    

commands:
  postCreateCommand: "cd src/backend/devopsRemoting;go mod tidy;echo 'go init dome'"