---
description: 
alwaysApply: true
enabled: true
---

# 新功能必须编写测试用例

## 规则

每次对 `src/agent/agent/` 项目新增功能或修改现有功能后，必须为可测试的函数编写单元测试。

## 测试规范

1. **测试风格**: 使用 table-driven + `t.Run()` 风格，手动 `if/t.Errorf` 断言
2. **文件命名**: 测试文件与源文件同目录，命名为 `xxx_test.go`；平台特定测试加 build tag（如 `status_linux_test.go`）
3. **文件系统**: 使用 `t.TempDir()` 创建临时目录，不依赖外部文件
4. **全局状态**: 测试前保存、测试后通过 `defer` 恢复（如 `useChinese`、环境变量等）
5. **进程相关**: 使用 `os.Getpid()` 测试存活进程，使用极大 PID (如 99999999) 测试不存在的进程

## 需要编写测试的改动

1. **新增纯函数或工具函数** — 必须编写测试（如 `splitUserDomain`、`readProperty`、`msg`/`msgf`）
2. **新增 CLI 子命令** — 测试可分离的辅助逻辑（如参数解析、文件清理、preserveSet）
3. **新增配置解析** — 测试配置读取和默认值
4. **新增状态检测** — 测试状态输出格式和边界条件
5. **修改现有函数签名或行为** — 更新对应测试用例

## 不需要测试的改动

- 需要 root/管理员权限的系统操作（systemd、sc.exe、launchd）
- 需要运行中服务的集成测试
- 单纯的日志文案调整
- 仅涉及外部 API 调用的改动

## 运行测试

```bash
# 修改后运行受影响包的测试
go test -v ./src/pkg/agentcli/

# 提交前运行全量测试
go test ./...
```
