parameters:
  - name: username
    type: string
    default: world

steps:
  - run: |
      echo "cyc_step_id_3 username ${{ parameters.username }}!"
#  - template: cyclic/step/templates/step4.yml
#    parameters:
#      username: STEP7
