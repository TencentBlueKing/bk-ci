version: v3.0
name: base-trigger-object-test
on:
  manual: enabled
  remote: enabled
  schedules:
    - cron: "0 0 1 * *"
      branches:
        - master
stages:
  - name: stage-1
    jobs:
      job1:
        name: test-job
        runs-on:
          pool-name: docker
        steps:
          - name: test-step
            uses: linuxScript@1.*
            with:
              script: "echo hello"
