# .ci/templates/stages.yml

parameters:
- name: username
  type: string
  default: world
- name: stagename
  type: String
  default: STAGENAME

stages:
- name: "stage_template_1"
  jobs:
    job_id_stage_template_1_1:
      name: my_job_1
      steps:
      - run: |
          echo "hello ${{ parameters.username }}"
    job_id_stage_template_1_2:
      name: my_job_2
      steps:
      - run: |
          echo "stage1, ${{ parameters.stagename }}"
- template: cyclic/stage/templates/stage1.yml
  parameters:
    username: CYC_STAGE1
