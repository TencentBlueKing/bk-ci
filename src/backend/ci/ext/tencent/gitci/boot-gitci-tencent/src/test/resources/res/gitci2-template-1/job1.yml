parameters:
- name: username
  type: string
  default: world
- name: jobname
  type: String
  default: JOBNAME

jobs:
  res_job_id_template_1:
    name: my_job_1
    steps:
    - run: |
        echo "stage1, job_id_1"
        echo "hello world!" >> "test.txt"
  template:
    - name: templates/job2.yml@repoB
      parameters:
        username: CYC_JOB1
    - name: cyclic/job/templates/job3.yml
  res_job_id_template_2:
    name: my_job_2
    steps:
    - run: |
        echo "hello ${{ parameters.username }}!"
    - run: |
        echo "hello ${{ parameters.jobname }} again!"
