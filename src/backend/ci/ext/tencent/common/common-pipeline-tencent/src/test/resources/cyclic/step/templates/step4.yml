parameters:
- name: username
  type: string
  default: world

steps:
- run: |
    echo "step4: ${{ parameters.username }}!"
- template: cyclic/step/templates/step8.yml
  parameters:
    username: STEP8
