parameters:
- name: username
  type: string
  default: world
- name: stepname
  type: String
  default: STEPNAME

steps:
- run: |
    echo "hello template ${{ parameters.username }}!"
- run: |
    echo "hello template ${{ parameters.stepname }} again!"
