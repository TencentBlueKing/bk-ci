parameters:
- name: username
  type: string
  default: world
- name: jobname
  type: String
  default: JOBNAME

jobs:
  job_id_template_cyc_1:
    name: my_job_1
    steps:
      - run: |
          echo "stage1, job_id_1"
  template:
    - name: cyclic/job/templates/job2.yml
      parameters:
        username: CYC_JOB2
