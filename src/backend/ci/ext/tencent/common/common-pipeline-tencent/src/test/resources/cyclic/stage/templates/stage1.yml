parameters:
- name: username
  type: string
  default: world
- name: stagename
  type: String
  default: STAGENAME

stages:
- template: cyclic/stage/templates/stage2.yml
  parameters:
    username: CYC_STAGE2
- name: "stage_template_1"
  jobs:
    job_id_stage_template_1_1:
      name: my_job_1
      steps:
      - run: |
          echo "hello ${{ parameters.username }}"
