parameters:
- name: username
  type: string
  default: world
- name: jobname
  type: String
  default: JOBNAME

jobs:
  job_id_template_cyc_5:
    name: my_job_5
    steps:
      - run: |
          echo "stage1, job_id_5"
#  template:
#    - name: cyclic/job/templates/job2.yml
#      parameters:
#        username: JOB
