parameters:
- name: username
  type: string
  default: world

steps:
- run: |
    echo "step5: ${{ parameters.username }}!"
- template: cyclic/step/templates/step8.yml
  parameters:
    username: CYC_STEP8
