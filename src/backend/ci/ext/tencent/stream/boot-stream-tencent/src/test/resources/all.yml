version: v2.0

on: delete

stages:
- name: stage-1
  check-in:
    reviews:
      flows:
        - name: 审批组1
          reviewers: [ "${{ci.actor}}" ]
        - name: 审批组2
          reviewers: ${{ci.actor}}
      variables:
        var_1:
          default: 1
          label: 中文
          type: SELECTOR
          values: [1,2,3,4]
      description: |
          说明下如何审核
          参数var_1如何取值
    gates:
      - template: epc_checks.yml
        parameters:
          receivers:
            - ruotiantang
            - ${{ci.actor}}
    timeout-hours: 10
  jobs:
    job_1:
      steps:
      - run: |
          echo "hello 1"
        with:
          sourceMirrorTicketPair:
            - host: 11
              username: 111
