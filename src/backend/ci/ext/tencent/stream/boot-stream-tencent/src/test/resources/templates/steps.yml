parameters:
  - name: username
    type: string
    default: world
    values:
      - false
      - true
  - name: stepname
    type: number
    default: STEPNAME
    values:
      - 1
      - 2
      - 3

steps:
  - run: |
      echo "template_step_id_1 username ${{ parameters.username }}!"
    continue-on-error: ${{ parameters.username }}
    retry-times: ${{ parameters.stepname }}
#  - template: cyclic/step/templates/step1.yml
#    parameters:
#      username: CYC_STEP1
#  - run: |
#      echo "template_step_id_2 stepname ${{ parameters.stepname }} again!"
