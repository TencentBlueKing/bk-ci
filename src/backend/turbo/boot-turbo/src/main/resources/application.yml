spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: turbo${service-suffix}
    desc: turbo
    version: 4.0.0
    packageName: com.tencent.devops.turbo

server:
  port: 21955

logging:
  file:
    path: ${service.log.dir}
