# 构建机层服务分发
location ~ ^/(ms/|)bkrepo/api/build/(.*)$ {
    # 设置auth的变量
    auth_request /auth/build;
    include auth.request.set.build.conf;

    # 路由
    set $access_type 'build';
    set $service "bkrepo";
    set $target '';
    access_by_lua_file 'conf/lua/router_srv.lua';

    # 设置proxy header的变量
    include set.bkrepo.conf;
    include proxy.set.header.build.conf;

    # 限速
    proxy_buffering on;
    limit_rate 50m;

    if ($request_uri ~* ^/(ms/|)bkrepo/api/build/(.*)$) {
        proxy_pass http://$target/$2;
    }
}


# user层服务分发
location ~ ^/(ms/|)bkrepo/api/user/(.*)$ {
    set_by_lua_block $target {
        local is_devx = string.find(ngx.var.http_host, "devx") ~= nil
        if is_devx then
            return "bkrepo.devx.tencent.com"
        else
            return config.bkrepo.domain
        end
    }
    rewrite ^/(ms/|)bkrepo/api/user/(.*)$ https://$target/web/$2 redirect;
}

# 微服务层服务分发
location ~ ^/(ms/|)bkrepo/api/service/(.*)$ {
    # 设置auth的变量
    auth_request /auth/service;
    include auth.request.set.service.conf;

    # 路由
    set $access_type 'service';
    set $service "bkrepo";
    set $target '';
    access_by_lua_file 'conf/lua/router_srv.lua';

    # 设置proxy header的变量
    include set.bkrepo.conf;
    include proxy.set.header.service.conf;

    # 限速
    proxy_buffering on;
    limit_rate 50m;

    if ($request_uri ~* ^/(ms/|)bkrepo/api/service/(.*)$) {
        proxy_pass http://$target/$2;
    }
}

# 外部访问层服务分发
location ~ ^/(ms/|)bkrepo/api/external/(.*)$ {
    # 路由
    set $access_type 'external';
    set $service "bkrepo";
    set $target '';
    access_by_lua_file 'conf/lua/router_srv.lua';

    # 设置proxy header的变量
    include set.bkrepo.conf;
    include proxy.set.header.common.conf;

    # 限速
    proxy_buffering on;
    limit_rate 50m;

    if ($request_uri ~* ^/(ms/|)bkrepo/api/external/(.*)$) {
        proxy_pass http://$target/$2;
    }
}

# bkrepo的build层服务分发, atom原子市场(new , 临时方案 , 需要插件商城直连bkrepo)
location ~ /repo/storge/build/atom/(.*) {
    include method.get.only.conf;
    auth_request /auth/build;

    # 设置proxy header的变量
    include proxy.set.header.build.conf;
    include auth.request.set.build.conf;

    # 域名
    set_by_lua_block $domain {
        if ngx.var.http_x_devops_bkrepo_mode == 'dl' then
            return config.bkrepo.dl_domain
        else
            return config.bkrepo.domain
        end
    }
    proxy_set_header Host $domain;
    proxy_set_header Authorization "Basic Z19ia3N0b3JlOmJrc3RvcmUyMDIx";

    set $access_type 'build';
    set $sub_path $1;

    # 反向代理到目标ip，端口，路径和参数
    proxy_pass http://$domain/generic/ext/bkstore/atom/$sub_path?$args;
}

# bk-plugin-fe下载
location ~ ^/bk-plugin-fe/(.*) {
    include method.get.only.conf;
    set $sub_path $1;

    set_by_lua_block $domain {
        return config.bkrepo.static_domain
    }

    header_filter_by_lua_block {
        if string.find(ngx.var.sub_path,"html$") then
            ngx.header["Cache-Control"] = "no-cache"
        else
            ngx.header["Cache-Control"] = "max-age=86400"
        end
    }

    # 设置proxy header的变量
    include proxy.set.header.common.conf;

    more_set_headers "Content-Disposition: ";

    # 反向代理到目标ip，端口，路径和参数
    proxy_pass http://$domain/bk-store/bk-plugin-fe/$sub_path?$args;
}
